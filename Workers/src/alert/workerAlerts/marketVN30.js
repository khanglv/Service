const socket = require('../../socket/turnOn');
const channelEvent = require("../channelSubscribe.json");

function sendSocket(){
    console.log("sdgjoidfjho", socket);
    // socket.io.emit('ceilingCrossChannel', `Warning: ${payload.s} đụng trần rồi!!!`);
}

sendSocket();

